<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_page">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="66dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageView
            android:id="@+id/imageView6"
            android:layout_width="41dp"
            android:layout_height="47dp"
            android:background="#7C53C3"
            android:tint="#FFFFFF"
            app:srcCompat="@drawable/ic_arrow_back" />

        <TextView
            android:id="@+id/textstyle"
            android:layout_width="388dp"
            android:layout_height="47dp"
            android:background="#7C53C3"
            android:gravity="center_vertical"
            android:text="Messagerie"
            android:textStyle="bold"
            android:textColor="#F6F2F2"
            android:textSize="25sp" />

    </LinearLayout>

    <!-- Recherche -->
    <LinearLayout
        android:id="@+id/rechercheLayout"
        android:layout_width="match_parent"
        android:layout_height="59dp"
        android:background="@drawable/bg_card"
        android:orientation="horizontal"
        android:layout_margin="8dp"
        app:layout_constraintTop_toBottomOf="@+id/headerLayout"
        app:layout_constraintStart_toStartOf="parent">

        <ImageView
            android:layout_width="60dp"
            android:layout_height="match_parent"
            app:srcCompat="@drawable/search" />

        <EditText
            android:id="@+id/editRecherche"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:hint="Rechercher un livreur ..." />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/containerLivreurs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/rechercheLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>